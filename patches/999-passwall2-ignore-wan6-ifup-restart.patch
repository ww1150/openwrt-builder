--- a/luci-app-passwall2/root/etc/hotplug.d/iface/98-passwall2
+++ b/luci-app-passwall2/root/etc/hotplug.d/iface/98-passwall2
@@ -2,6 +2,9 @@
 
 [ "$ACTION" = "ifup" ] || [ "$ACTION" = "ifupdate" ] || exit 0
 
+# WAN6 ifupdate refreshes nftsets; restarting on ifup interrupts both proxies.
+[ "$INTERFACE" = "wan6" ] && [ "$ACTION" = "ifup" ] && exit 0
+
 . /usr/share/passwall2/utils.sh
 
 ([ "$(get_cache_var "ENABLED_DEFAULT_ACL")" = "1" ] || [ "$(get_cache_var "ENABLED_ACLS")" = "1" ]) && [ -f ${LOCK_PATH}/${CONFIG}_ready.lock ] && {
